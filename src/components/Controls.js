import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  text-align:center;
  padding: 12px;
`;

const Controls = ({
  onClear,
  onSeed,
  toggleStartStop,
  running,
  onStep,
}) => (
  <Wrapper>
    {/* // TODO: implement buttons that call event handlers */}
  </Wrapper>
);
export default Controls;
